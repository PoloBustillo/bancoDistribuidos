name: Deploy Banco Distribuido (Docker)

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite ejecutar manualmente

env:
  REGISTRY: ghcr.io
  IMAGE_NAME_COORDINADOR: ${{ github.repository }}-coordinador
  IMAGE_NAME_WORKER: ${{ github.repository }}-worker
  DEPLOY_PATH: "/home/polo/banco-distribuido"

jobs:
  # Job 1: Build y Push a GHCR
  build-and-push:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Coordinador
        id: meta-coordinador
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_COORDINADOR }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Coordinador image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./coordinador/Dockerfile
          push: true
          tags: ${{ steps.meta-coordinador.outputs.tags }}
          labels: ${{ steps.meta-coordinador.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract metadata for Worker
        id: meta-worker
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_WORKER }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Worker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./worker/Dockerfile
          push: true
          tags: ${{ steps.meta-worker.outputs.tags }}
          labels: ${{ steps.meta-worker.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Job 2: Deploy al servidor
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            set -e  # Exit on error

            echo "🚀 Iniciando deployment con Docker..."

            # Crear directorio si no existe
            mkdir -p ${{ env.DEPLOY_PATH }}
            cd ${{ env.DEPLOY_PATH }}

            # Clonar o actualizar repositorio
            if [ -d ".git" ]; then
              echo "📦 Actualizando repositorio..."
              git fetch origin
              git reset --hard origin/main
              git pull origin main
            else
              echo "📦 Clonando repositorio..."
              rm -rf * .* 2>/dev/null || true
              git clone https://github.com/${{ github.repository }} .
            fi

            # Verificar que Docker esté instalado
            if ! command -v docker &> /dev/null; then
              echo "❌ Docker no está instalado. Por favor instala Docker primero."
              exit 1
            fi

            # Verificar que Docker Compose esté instalado
            if ! docker compose version &> /dev/null; then
              echo "❌ Docker Compose no está instalado. Por favor instala Docker Compose primero."
              exit 1
            fi

            # Crear archivo .env si no existe
            if [ ! -f .env ]; then
              echo "📝 Creando archivo .env..."
              cp .env.example .env
              echo "⚠️  IMPORTANTE: Configura DATABASE_URL en .env con tu base de datos existente"
              echo "⚠️  Ejemplo: DATABASE_URL=postgresql://banco_user:password@localhost:5432/banco"
            fi

            # Verificar que DATABASE_URL esté configurado
            if ! grep -q "^DATABASE_URL=" .env || grep -q "tu_password_aqui" .env; then
              echo "❌ ERROR: DATABASE_URL no está configurado correctamente en .env"
              echo "Por favor configura DATABASE_URL con la conexión a tu base de datos PostgreSQL"
              exit 1
            fi

            # Crear carpeta de backups
            mkdir -p backups

            # Crear carpeta de logs
            mkdir -p logs

            echo "🛑 Deteniendo contenedores existentes..."
            docker compose down --remove-orphans || true

            echo "� Login a GitHub Container Registry..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "📦 Pulling latest images from GHCR..."
            docker compose pull

            echo "🚀 Iniciando servicios con Docker Compose..."
            docker compose up -d --no-color 2>&1

            echo "⏳ Esperando que los servicios estén listos..."
            sleep 15
            
            echo "📊 Estado de contenedores:"
            docker ps --filter "name=banco-" --format "table {{.Names}}\t{{.Status}}" 2>&1 || echo "Contenedores iniciando..."

            echo "🧹 Limpiando imágenes antiguas..."
            timeout 30 docker image prune -af --filter "until=24h" > /dev/null 2>&1 || true

            echo "✅ Deployment completado!"

      - name: Verificar Health de los servicios
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 1m
          script: |
            cd ${{ env.DEPLOY_PATH }}

            echo "🏥 Verificando salud de los servicios..."
            echo ""
            echo "📊 Estado de contenedores:"
            docker ps --filter "name=banco-" --format "table {{.Names}}\t{{.Status}}" 2>&1 || echo "Contenedores iniciando..."
            echo ""

            # Dar tiempo para health checks
            sleep 5

            # Verificar servicios HTTP con timeout corto
            echo "Verificando endpoints HTTP..."
            timeout 3 curl -sf http://localhost:4000/health > /dev/null 2>&1 && echo "✅ Coordinador (4000)" || echo "⚠️  Coordinador iniciando"
            timeout 3 curl -sf http://localhost:3001/api/health > /dev/null 2>&1 && echo "✅ Worker 1 (3001)" || echo "⚠️  Worker 1 iniciando"
            timeout 3 curl -sf http://localhost:3002/api/health > /dev/null 2>&1 && echo "✅ Worker 2 (3002)" || echo "⚠️  Worker 2 iniciando"
            timeout 3 curl -sf http://localhost:3003/api/health > /dev/null 2>&1 && echo "✅ Worker 3 (3003)" || echo "⚠️  Worker 3 iniciando"

            echo ""
            echo "📋 Logs recientes:"
            timeout 10 docker compose logs --tail=15 --no-color 2>&1 || echo "Logs no disponibles"

      - name: Rollback en caso de fallo
        if: failure()
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 2m
          script: |
            cd ${{ env.DEPLOY_PATH }}

            echo "❌ Deployment falló. Ejecutando rollback..."

            # Mostrar logs completos para debugging
            echo "📋 Logs de los servicios:"
            docker compose logs --tail=100 --no-color || true

            # Detener servicios fallidos
            docker compose down --remove-orphans || true

            echo "🔙 Rollback completado. Los servicios han sido detenidos."
            echo "⚠️  Revisa los logs arriba para identificar el problema."

      - name: Notificación de éxito
        if: success()
        run: |
          echo "✅ Deployment exitoso con Docker y GHCR!"
          echo "🐳 Todos los contenedores están corriendo"
          echo "� Imágenes desde: ghcr.io/${{ github.repository }}"
          echo ""
          echo "📊 Servicios disponibles:"
          echo "  - Coordinador: puerto 4000"
          echo "  - Worker 1: puerto 3001"
          echo "  - Worker 2: puerto 3002"
          echo "  - Worker 3: puerto 3003"
          echo ""
          echo "� Ver imágenes: https://github.com/${{ github.repository }}/pkgs/container/bancodistribuidos-coordinador"
