# PostgreSQL Database (CONFIGURAR CON TU BASE DE DATOS EXTERNA)
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# 
# Ejemplos:
# - Si la BD está en localhost del servidor: postgresql://banco_user:password@localhost:5432/banco
# - Si la BD está en otro servidor: postgresql://banco_user:password@192.168.1.100:5432/banco
# - Si la BD está en la nube: postgresql://banco_user:password@db.ejemplo.com:5432/banco
DATABASE_URL=postgresql://banco_user:tu_password_aqui@localhost:5432/banco

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_secreto_super_seguro_aqui_cambiar_en_produccion

# Node Environment
NODE_ENV=production

# CORS Origins (separados por comas)
# Incluye todos los dominios desde donde se accederá al backend
CORS_ORIGIN=https://banco-distribuidos.vercel.app,https://banco.psic-danieladiaz.com

# Puertos de los servicios
COORDINADOR_PORT=4000
WORKER_1_PORT=3001
WORKER_2_PORT=3002
WORKER_3_PORT=3003

# NOTA: Si tu base de datos está en localhost del servidor y tienes problemas
# de conexión desde Docker, puedes descomentar network_mode: "host" en los workers
# del docker-compose.yml
